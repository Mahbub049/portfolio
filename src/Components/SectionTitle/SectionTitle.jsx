const SectionTitle = ({heading}) => {
    return (
        <div className="my-8" data-aos="zoom-out-down">
            <h2 className="text-4xl text-center font-extrabold text-[#4690ff]">{heading}</h2>
        </div>
    );
};

export default SectionTitle;